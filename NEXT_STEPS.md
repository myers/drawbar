# Next Steps

- End-to-end test of ZFS snapshot cache against a real cluster with OpenEBS ZFS LocalPV
- `container: options:` support (e.g., `--tmpfs /tmp:exec,noatime`)
- End-to-end test against a real Forgejo instance with Forgejo's own CI workflows
- Helm chart updates: snapshot config values, RBAC for volumesnapshots + PVCs
